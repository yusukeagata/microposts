<%= form_for(current_user.favorite_relations.find_by(micropost_id: micropost.id),
             html: {method: :delete},
             remote: true) do |f| %>
  <%= f.submit "Unfavorite", class: "btn btn-primary" %>
<% end %>